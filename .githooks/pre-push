#!/bin/sh
# Pre-push hook: delegate to `make check` so the quality gate is defined
# once (in the Makefile) and invoked identically by this hook, by the
# /commit skill, and by any manual invocation. Bypass: git push --no-verify.

set -e
echo "Running pre-push checks (make check)..."
make check
echo "All checks passed."
